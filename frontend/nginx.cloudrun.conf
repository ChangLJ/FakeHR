# Cloud Run 前端：僅提供靜態檔，API 由瀏覽器直連 VITE_API_URL（需 API 設定 CORS）
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
